version: "3.9"

services:
  snowflake-cortex-chat:
    build: .
    container_name: snowflake-cortex-chat
    ports:
      - "8000:8000"
      - "8501:8501"
    env_file:
      - .env
    restart: unless-stopped